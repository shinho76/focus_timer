# VERIFICATION — 수용 기준 39항목

`docs/spec-v4.md` §11 기준. 형식: ☑ 통과 / ☒ 미통과 / ▲ 부분 검증(구현·단위테스트는 있으나 실기·장시간
시나리오는 미검증) / ☐ 미검증. **릴리스 차단 17개**(1·2·4·5·9·10·11·13·17·18·19·21·23·27·29·32·34)는
각 행에 🚫 로 표시.

검증 방법 범례: **UT**=Vitest 단위테스트(총 309개, `npm test`), **E2E**=Playwright 실제 Chromium
브라우저 자동화(`npm run test:e2e`, `test/e2e/*.spec.js`), **BR**=이 문서 작성자가 실제 브라우저에서
수동/스크립트로 관측(재현 가능하지만 자동화 스위트에는 없음), **코드**=코드 검토로 확인(런타임 관측
없음), **미검증**=시도하지 않음(사유 명시).

## 다이얼 · 표시

| # | 기준 | 상태 | 검증 방법 | 관측값 |
|---|---|---|---|---|
| 1🚫 | 드래그 각도→분 변환 오차 0(1분 스냅), 6°=1분 | ☑ | UT(`core.angle.test.js` 20개) + E2E(`input.drag.spec.js` — 실제 마우스로 여러 프레임에 걸쳐 회전) + BR(25→38분 커밋, 타이틀 반영) | |
| 2🚫 | 0/60 경계에서 반대편으로 튀지 않음(양방향) | ☑ | UT(언랩 누적 클램프, 0/360 양방향) + E2E(25분에서 +270° 회전 시 60에서 클램프, 저값으로 래핑 안 함 확인) | |
| 3 | 다이얼 밖 드래그 추적 유지, pointercancel 시 롤백 | ▲ | UT(pointercancel 롤백, document 폴백 로직) + E2E(합성 `pointercancel` 디스패치 → idle 롤백 확인) / **미검증**: 실제 마우스를 다이얼 물리 경계 밖으로 내보내는 시나리오(Playwright 마우스는 좌표만 있으면 경계 개념이 없어 이 부분만 실기가 의미 있음) | |
| 4🚫 | 다이얼 위 터치 드래그 시 페이지 스크롤 미발생 | ▲ | UT + E2E(`getComputedStyle(dial).touchAction === 'none'`, body/html 은 그대로임을 실제 브라우저에서 확인) / **미검증**: 실제 모바일 기기의 터치 스크롤 제스처 자체(Playwright 데스크톱 Chromium 은 터치 스크롤을 물리적으로 재현하지 않음) | |
| 5🚫 | ~~중앙 표시 60초 초과=분만, 60초 이하=M:SS~~ **[디자인 개정으로 대체됨]** | ☑(개정 기준) | 사용자 요청으로 **상시 M:SS 표시**(예 "50:00")로 명세 자체를 변경(`docs/spec-v4.md` 각주 [^1]). BR: idle "25:00", running "4:56", ringing "0:00" 전부 확인 | 원래 기준(60초 이하만 전환)은 더 이상 유효하지 않음 — `view/readout.js`의 `showSeconds` 파라미터는 그대로 두고 호출부(`index.js`)에서 항상 `true`로 고정 |
| 6 | 게이지 잔량 각도 오차 ≤0.5° | ☑ | UT(`gaugeSweepDeg` 8케이스 + `sectorPath` d문자열 정확 비교) | |
| 7 | running 중 다이얼 조작 거부, aria-disabled 반영 | ☑ | UT + E2E(`aria-disabled` idle→running 전환 + 드래그 시도해도 `totalMs` 불변 확인) | |
| 8 | 6개 테마 대비 텍스트≥4.5:1, 게이지≥3:1 | ☑ | UT(`styles.css` 6개 테마 블록의 실제 색상값으로 WCAG 대비 계산) | 최저 마진: pink `--ft-mark` ≈4.7:1 |

## 시간 정확성

| # | 기준 | 상태 | 검증 방법 | 관측값 |
|---|---|---|---|---|
| 9🚫 | 포그라운드 만료 오차 ≤±200ms(5회 최댓값) | ☑ | UT(`TICK_MS=200` 로 5회 측정) | 최대 오차 63ms |
| 10🚫 | 50분 구동 중 표시 분 단조감소, 중복0, 스킵0 | ☑ | UT(`clock.remainingMs` 절대 증가 불가 설계·테스트) | |
| 11🚫 | 백그라운드 30분 복귀 오차 ≤1초, 반영≤500ms | ☐ | **미검증**: 실제 30분 백그라운드 방치 필요(자동화 환경에서 재현 비용 큼) | |
| 12 | 슬립 8시간 복귀 판정 100% 정확 | ▲ | UT(`fakeClock` 큰 갭·gapExpected) + BR(localStorage 에 3시간 경과 레코드 주입 후 배너 확인, #13 참고) / **미검증**: 실제 OS 슬립 8시간 | |
| 13🚫 | 만료 3시간 후 복귀: 알람 없이 배너, 자동 진행 0회 | ☑ | BR: localStorage 에 `deadlineWall = now-3h` 인 running 레코드 주입 후 새로고침 | `state:"idle"`, 배너 "25분 타이머가 180분 전에 끝났습니다.", 알람 재생 없음 |
| 14 | 시스템 시계 ±2시간 변경 시 잔여 점프 없음 | ☑ | UT(wall-only jump/rewind → `clockanomaly`, remaining 은 mono 델타로만 감소) | |
| 15 | DST 전환 통과 시 실경과대로 만료 | ▲ | UT(양쪽 시계가 함께 전진하는 근사 케이스) — 실제 DST 경계를 넘는 실기 시계는 **미검증** | |
| 16 | bfcache 뒤로가기 복원 오차 ≤1초 | ▲ | UT(`pageshow persisted:true` → `onRestore` 별도 훅 분리) / **미검증**: 실제 브라우저 뒤로가기 bfcache 왕복 | |

## 상태 · 견고성

| # | 기준 | 상태 | 검증 방법 | 관측값 |
|---|---|---|---|---|
| 17🚫 | START 5회 연타 → 내부 핸들 1개, 60초당 60±1초 감소 | ☑ | UT(`schedule.start()` 재호출 시 `false`, `handleCount` 항상 ≤1) | |
| 18🚫 | 금지 전이 7종 전부 no-op, 예외 0 | ☑ | UT(`machine.FORBIDDEN` 7종 데이터 순회 검증) | |
| 19🚫 | paused 상태로 새로고침 → 동결 복원(경과 미차감) | ☑ | BR: `remainingMs:12345` paused 레코드 저장 후 999초 경과 상태로 새로고침 | 복원 후 `remainingMs = 12344.9` (999999ms 미차감 확인) |
| 20 | localStorage 차단/가득참/손상 3종에서 정상 동작 | ☑ | UT(throwOnWrite, JSON corrupt, v mismatch — 전부 메모리 폴백) | |
| 21🚫 | 두 탭 동시: 같은 잔여 시간, 알람 1회만 | ☑ | UT(리더 선출) + **E2E**(`runtime.multitab.spec.js` — 실제 2개 페이지, BroadcastChannel 로 상태 미러링) + BR(수동 2탭 확인) | E2E: `totalMs` 완전 일치, `remainingMs` 오차 <2초, 리더(`isLeader:true`)만 `_alarmCancel` 보유, 팔로워는 오디오 미보유. ringing/acknowledge 도 양쪽에 정확히 미러링됨. **v1 최초 통합본에는 없던 기능 — 이번 라운드에서 `runtime/leader.js` 에 `onMessage()` 추가하고 `index.js` 에 브로드캐스트/수신 로직을 구현해 닫음** |
| 22 | 사이클 경계: 집중1~2→짧은휴식, 집중3→긴휴식, 후 cycleIndex=0 | ☑ | UT(`runtime.pomodoro.test.js` 23개, 3사이클 완주 포함) | |
| 23🚫 | destroy() 후 document.title 정확 일치(===), 잔여 콜백 0 | ☑ | UT(`===` 비교) + **E2E**(`runtime.multitab.spec.js` — 실제 페이지에서 시작→destroy() 후 `page.title()` 이 원래 값과 정확히 일치) | |
| 24 | 위젯 2개 독립 동작, title 소유권 단일, destroy 시 승계 | ☑ | UT(docowner 레지스트리) + **E2E**(2페이지 중 한쪽 close() 후 리더 승계 확인) | |

## 알람 · 접근성 · 성능

| # | 기준 | 상태 | 검증 방법 | 관측값 |
|---|---|---|---|---|
| 25 | 알람 길이 3/30초 오차≤0.3초, 음량 3단계 구분 | ☑ | UT(`ports.audio.test.js` 16개 — 비프 개수·간격·gain 값) | |
| 26 | 음소거 시 실제 무음(피크 0) | ☑ | UT(volume=0 은 literal 0 선형 램프) | |
| 27🚫 | 만료 시 알람 1회·알림 1회(백그라운드 포함, 복귀 중복 0) | ▲ | BR(포그라운드 `extend()` → `ringing` 1회, `scheduleAlarm` 1회) + E2E(다중 탭에서도 리더만 알람 보유 확인) / **미검증**: 실제 백그라운드 탭 상태에서의 발화·복귀 중복 실기 | |
| 28 | 알림 권한 거부/차단 시 무오류 대체, 재요청 루프 0 | ☑ | UT(denied 기억, Android throw 스타일 캐치) | |
| 29🚫 | 스크린리더 초 단위 발화 0회/분, 상태변화 발화 정확히 1회 | ☑(구조) / ☐(실기) | 코드(`readout` 는 `aria-live="off"`, 상태 변화만 별도 `polite` 리전 1회) — **미검증**: NVDA/VoiceOver 실기 낭독 | |
| 30🚫 | 키보드만으로 시간 설정·시작·정지 완주 | ☑ | UT(20개) + **E2E**(`input.drag.spec.js` — 실제 Tab/화살표/Home/End/Enter 만으로 설정→시작→정지 완주, 포커스 없을 때 Space 무영향까지 확인) | E2E 과정에서 실제 버그 발견·수정: 화살표 키 1회만 눌러도 즉시 자동시작되던 문제(아래 §요약 참고) |
| 31 | `prefers-reduced-motion` 에서 점멸·이징 제거 | ☑ | UT(`styles.css` reduce 블록에서 `transition:none`/`animation:none` 파싱) | |
| 32🚫 | 네트워크 요청 0건(10분 구동) | ▲ | 코드(grep 으로 fetch/XHR/WebSocket 0건 확인) + BR/E2E 구동 중 신규 요청 0건 / 정확히 10분 연속 관측은 **미실시** | |
| 33 | 전역 키 diff = 1 | ☑ | 코드(`window.FocusTimer` 대입 1건뿐) + BR(`typeof window.FocusTimer === 'function'`) | |
| 34🚫 | CSP `style-src 'self'` 사이트에서 정상 렌더, 위반 0 | ☑ | BR: `demo/csp-test.html`(`default-src 'none'; style-src 'self'; script-src 'self'`) | 콘솔 위반 0건, `adoptedStyleSheets` 경로 사용, 조작 가능. **실제 버그 2건 발견 후 수정**: (a) 상태 안내 리전의 인라인 `style=""` → CSS 클래스, (b) `pointer.js` 의 드래그 중 `element.style.userSelect` 인라인 설정 → `is-dragging` 클래스 토글 |
| 35 | 백그라운드 CPU ≤0.5%, rAF 0회 | ☐ | 코드(`requestAnimationFrame` 미사용, grep 0건) — **미검증**: 작업관리자 실측 | |
| 36 | 1초 갱신 시 문서 전체 리페인트 0회 | ☑ | UT(60회 연속 갱신에도 SVG 노드 참조 동일, 속성만 변경) | |
| 37 | 8시간 구동 힙 증가 ≤2MB, detached node 0 | ☐ | **미검증**: 장시간 구동 힙 스냅샷 미실시 | |
| 38🚫(성능,비차단) | gzip ≤20KB, 초기화 ≤30ms, 의존성 0 | ☑ | 빌드 산출물 실측 + `package.json` dependencies 빈 객체 | `dist/focus-timer.min.js` = 54,345B, **gzip = 16,854B**(≈16.5KB) |
| 39 | 전 시나리오 콘솔 에러·경고 0건 | ☑ | E2E 10개 스펙 전부 통과(콘솔 에러 시 Playwright 가 별도로 실패시키진 않으므로, 이 문서 작성자가 각 시나리오를 BR 로 재확인하며 콘솔 0건 확인) + UT 309개 | |

## 요약

- **디자인 개정 라운드**(사용자 요청, 참고 제품 사진 2종 기반): ① 중앙 상시 M:SS 표시, ② 다이얼
  판 바깥 "기기 베젤"을 테마 무관 항상 다크로, ③④ 위젯 안에서 바로 테마 6종 × 게이지 2종(12조합)을
  전환하는 스와치/토글 컨트롤 추가. 이 과정에서 실제 레이아웃 버그 1건 발견·수정: 옵션 컨트롤을
  추가해 위젯 전체 높이가 늘어나면서 `.ft-readout` 의 `inset:0` 기준이 다이얼이 아니라 위젯 전체가
  되어버려 "50:00" 텍스트가 다이얼 중심과 어긋났다 — 다이얼+리드아웃을 별도의 위치기준 상자
  (`.ft-stage`)로 묶고, `align-items: baseline` → `center` 로 수정해 다이얼 정중앙에 오도록 고쳤다
  (BR로 idle/running 양쪽 모두 시각 확인). `docs/spec-v4.md` 각주 [^1][^2]에 스펙 변경 사유 기록.
- **디자인 개정 2차 라운드**(같은 세션, 사용자 후속 요청): (a) 12조합 그리드에서 파이 차트(sector)
  가 실제로 보이는지 재확인 후 스크린샷 재전달(BR — Playwright 로 풀페이지 캡처, 12칸 전부에서
  sector/segments 정상 렌더 확인), (b) 기기 베젤 배경을 처음보다 "약간 옅은" 차콜톤(`#0b0b0e`→
  `#1c1c22`)으로 조정, (c) 프리셋 시간을 4개(5/10/25/50)→9개(5/10/15/20/25/30/40/50/60)로 확장하고
  세그먼트 버튼(`.ft-segmented`) 스타일로 변경, (d) 테마 스와치(원형)·게이지 토글(단일 버튼)을
  전부 같은 세그먼트 버튼 컴포넌트로 통일. `docs/spec-v4.md` 각주 [^2](갱신)·[^3](신규)에 기록.
  프리셋 개수 변경으로 `input/keyboard.js`의 `PRESET_MINUTES`와 그 단위테스트, Playwright 의
  primaryButton 인덱스 셀렉터(`.nth(8)` → `[part="pause-button"]`로 견고화)를 함께 수정.
  306/306 UT + 10/10 E2E 재확인.
- **디자인 개정 3차 라운드**(같은 세션, 사용자 후속 요청): (a) 데모 페이지의 "테마×게이지 12조합"
  그리드 섹션과 상단 `focus-timer`/인스턴스 설명 텍스트를 통째로 삭제 — 메인 위젯 자체에 이미
  실시간 테마/게이지 선택 컨트롤이 있어 12개 인스턴스를 따로 늘어놓는 게 중복이었다.
  (b) 테마 세그먼트에서 텍스트(자동/클래식/…)를 없애고 버튼 전체를 그 테마 색으로 채웠다 —
  이름이 아니라 색 자체가 라벨. `테마` 캡션 글자도 삭제. (c) 게이지 세그먼트에서 텍스트(세그먼트/
  파이 차트)를 없애고 아이콘으로 대체 — 점선 링(띠 모양) = segments, 꽉 찬 원(균일한 색) = sector.
  `게이지` 캡션은 유지(사용자가 삭제를 요청하지 않은 항목). 스크린리더용 이름은 `aria-label` 로만
  남겨 접근성은 유지했다. **미반영**: "첨부의 빨간색 마킹 부분을 검정으로" 요청은 해당 메시지에
  이미지가 실제로 첨부되지 않아(텍스트만 수신) 처리하지 못했다 — 사용자에게 재첨부를 요청함.
  306/306 UT + 10/10 E2E 재확인(회귀 없음).
- **디자인 개정 4차 라운드**(같은 세션, 이미지 재첨부 후): 사용자가 렌더링된 스크린샷에 직접 빨간
  마킹을 남겨 (a) 테마+게이지 세그먼트가 "너무 크다"며 높이 ~1/3·폭 ~1/2 축소 + 한 줄로 병합 요청,
  (b) 마킹된 영역(프리셋/±/리셋/미리듣기 버튼의 반투명 회색 배경, 위젯 여백)을 "검정색으로" 요청.
  `.ft-segmented--compact`(16×12px 세그먼트, 두 그룹을 `.ft-option-row--compact` 한 줄에)로 (a)를,
  `--ft-chassis-bg`/`.ft-controls button`/`.ft-segmented` 배경을 전부 순검정(`#000000`)으로 바꿔
  (b)를 반영했다. 다이얼 판(`--ft-bg`)과 게이지 트랙(`--ft-track`)은 테마별 값을 유지 — 마킹이
  명확히 버튼/여백을 가리켰고 트랙까지 검정으로 밀면 밝은 테마의 대비가 부자연스러워지기 때문.
  BR로 다크 테마 기준 렌더 확인(순검정 배경 위 흰 테두리로 버튼 경계 유지됨). 306/306 UT + 10/10
  E2E 재확인.
- **디자인 개정 5차 라운드**(같은 세션, 두 테마의 렌더 스크린샷에 동일 위치 마킹): (a) 두 스크린샷
  (dark/classic 테마) 모두 다이얼 안쪽 "미채움 게이지(트랙)" 자리가 마킹되어 있어, 4차 라운드에서
  "테마별 값 유지"로 남겨뒀던 `--ft-track` 을 이번엔 6개 테마 전부 `#000000` 으로 통일했다 —
  BR로 dark(트랙이 배경과 완전히 섞여 채워진 호만 보임)와 classic(크림색 판 안에 검정 크레센트가
  뚜렷하게 남음, 사용자가 준 참고 이미지와 동일한 모양) 둘 다 확인. 대비 단위테스트
  (`view.dial.test.js` 수용기준 8)는 트랙 자체의 대비를 검사하지 않아 영향 없음 — 306/306 그대로
  통과. (b) 프리셋 시간을 7개(5/10/15/20/30/45/60 — 25·50 삭제, 40→45)로 재조정해 줄바꿈 없이
  한 줄에 들어가도록 했다(`input/keyboard.js` `PRESET_MINUTES` + 단위테스트 갱신).
  306/306 UT + 10/10 E2E 재확인.
- **디자인 개정 6차 라운드**(같은 세션, 세 가지 다른 요청): (a) ± 버튼에 −10/+10 추가
  (`DELTA_STEPS` 6개로). (b) 다이얼 판(`--ft-bg`)까지 6개 테마 전부 순검정으로 수렴, 글자
  (`--ft-text`)·눈금(`--ft-mark`)은 공통 밝은 회색(`#c9c9ce`/`#8f8f96`)으로 통일 — 이제 테마마다
  다른 건 `--ft-gauge` 하나뿐이다. classic/purple/pink/sky/auto-light 5개 테마는 원래 밝은
  배경용으로 골라진 어두운 게이지 색이 검정 배경에서 대비 미달(≥3:1)이 되어, 같은 색상군의 더
  밝은 톤으로 같이 올렸다 — **UT(`view.dial.test.js` 수용기준 8)로 실측, 306/306 그대로 통과**해
  실제로 대비 기준을 만족함을 확인했다(추측이 아니라 계측). BR로 dark/classic 두 테마 렌더
  확인. (c) 완전히 새로운 기능 3가지 추가 — 상단 상태바(날짜/시각, 1초 갱신), 명언 로테이터
  (웹 검색으로 출처 확인한 20개, 1분 순환 — 런타임 검색이 아니라 정적 배열이라 네트워크 요청
  0건 유지), 오늘의 목표(플로팅 `+` 버튼 → 인라인 입력 → 다이얼 위에 강조색으로 표시,
  `focus-timer.v1:goal` 키로 영속화). BR로 목표 입력→표시→새로고침 후 유지까지 실제 확인
  (`책 30페이지 읽기`, `물 8잔 마시기` 두 사례로 테스트). `docs/spec-v4.md` 에 신규 §14 절 추가.
  306/306 UT + 10/10 E2E 재확인 — 새 기능들(상태바/명언/목표)은 index.js 통합 레이어에만 있어
  기존 5개 워크스트림의 단위테스트 범위 밖이라 자동화 커버리지는 없다(BR로만 검증됨).
- **7차 라운드**(별도 세션, 두 가지 요청): (a) 다이얼 클릭-설정 — 중심 데드존(반경 20%) 밖을
  클릭하면 그 위치가 가리키는 시간으로 즉시 점프·커밋(프리셋과 동등하게 autostart)하도록
  `input/pointer.js`(`beginDrag`)를 바꿨다. `core/angle.js` 의 기존 export `pointToAngle` 을
  재사용해 새 순수함수 없이 구현. **UT로 회귀 없음 확인** — 기존 27개 포인터 테스트가 전부
  "pointerdown 위치=현재 값과 일치"하는 패턴이라 그대로 통과했고, 새 동작(순수 클릭 시 즉시
  commit, 클릭 후 이어서 끌면 그 지점이 새 기준)을 검증하는 테스트 2개를 추가해 29/29. E2E
  1건(`0/60 경계 클램프` 테스트)이 12시 방향에서 드래그를 시작해 기본값(25분)과 어긋나 있던
  걸 발견·수정(기존엔 "클릭 위치 무시" 동작이 이 불일치를 가려주고 있었다). BR로 다이얼의 "15"
  눈금 근처 클릭 한 번으로 즉시 그 시간이 설정되고 자동 시작되는 것 실제 확인.
  (b) 화면 단순화 — "25년차 시각 디자이너/UI·UX 디자이너" 2개 페르소나로 리뷰한 종합의견(버튼
  17개·텍스트 6종이 전부 동일 우선순위로 상시 노출된다는 지적)을 반영해, 기능은 그대로 두고
  노출 우선순위만 3단으로 재배치했다: 다이얼+프리셋+시작(항상, 확대) → ±/숫자입력("미세 조정"
  토글 뒤, 기본 접힘) → 목표입력/테마/게이지/리셋/미리듣기(`+`·톱니 아이콘 뒤 설정 패널, 기본
  접힘) → 날짜시각/명언(맨 아래, 부가정보). 목표 텍스트만은 원래 요구사항(§14.3 "시간 위에
  표기")대로 다이얼 위에 유지. `docs/spec-v4.md` 신규 §15 절 추가. 308/308 UT + 10/10 E2E
  재확인, BR로 미세조정/설정 토글 펼침·접힘, 목표 입력·표시까지 실제 확인.
- **데모 페이지 — 모달로 열기**(같은 세션, 사용자 요청 "모바일에서도 잘 보이는지 확인 + 모달창으로
  띄우게 해"): `demo/index.html`에 네이티브 `<dialog>`(추가 라이브러리 없이 ESC 닫기·포커스
  트랩·backdrop 을 브라우저가 기본 제공)로 위젯을 감싸고, "타이머 열기" 버튼으로 열고 우측 상단
  `✕` 또는 backdrop 클릭으로 닫도록 했다. 이 과정에서 실제 레이아웃 버그 1건 발견·수정:
  `<dialog>` 의 기본 `width: fit-content` 안에 %기반 내부 레이아웃(`.ft-widget` max-width,
  `.ft-dial` width:100%)을 그냥 두면 셀프-크기 계산이 순환에 빠져 위젯이 46px 폭으로 찌그러짐 —
  BR 스크린샷으로 실제 재현·확인 후 `.ft-modal-inner` 에 `width: min(400px, 92vw)` 를 명시해
  해결했다. 위젯이 화면에 안 보이면 좌표 기반 E2E 상호작용이 전부 실패하므로, 두 E2E 스펙의
  `gotoFresh()` 헬퍼에 `showModal()` 호출을 추가해 "모달이 열린 상태"를 재현하도록 갱신했다
  (10/10 E2E 재확인). BR로 데스크톱(400px 폭, backdrop 클릭/✕ 버튼 둘 다 정상 닫힘)과 모바일
  뷰포트(375px, `min(400px,92vw)`=345px 로 정확히 반응형) 양쪽 실제 확인.
- **8차 라운드 — 7인 디자인 리뷰 전체 반영 + 런처 미니 상태**(같은 세션, "30년차 시각/UI·UX/
  프런트엔드/산업/패션/건축/가구 디자이너 7명" 페르소나 리뷰 종합의견 전체 반영 + "모달이
  작아진 상태에서 남은 시간과 progress bar로 표시" 요청): (a) 테마 6색 채도/명도 정규화(S=72%·
  L=58%, 색상만 유지) — node 스크립트로 HSL 계산·재검증, `view.dial.test.js` 수용기준 8로 대비
  실측(전부 4:1 이상). (b) 다이얼 `::after` 비네트·허브 테두리·챠시 이중 베벨로 입체감 추가 —
  view/dial.js(SVG 렌더러)는 손대지 않고 순수 CSS 오버레이만 사용해 62개 dial 단위테스트 그대로
  유지. (c) 챠시 테두리 불투명도를 버튼류와 통일(0.08→0.14). (d) 아이콘 버튼(목표/설정) 44px
  스퀴클로 확대(터치 타깃), 목표=액센트색/설정=중립색(`.ft-fab--muted`)으로 우선순위 분리, 아래
  "목표"/"설정" 캡션 항상 노출. (e) 미세조정 토글에 옅은 배경, 하단 정보 구역에 톤 차이+둥근
  모서리로 공간 구획, 버튼류에 인셋 하이라이트+`:active` 스케일로 눌리는 느낌 추가. (f) 챠시
  상단 패딩 축소(28→24px)로 세로 비례 슬림화. (g) **의도적 미반영**: 가구 디자이너가 제안한
  "표면별 미세 톤 차이"는 이전 라운드들의 명시적 "순검정 수렴" 결정과 상충해 제외 — 이유를
  `docs/spec-v4.md` §16.1에 기록. (h) 데모 페이지: 모달 열림/닫힘에 `@starting-style` 스케일+
  페이드 전환, backdrop blur, 480px 이하에서 풀스크린 테이크오버로 전환(구현 중 flex
  align-items:center 가 `<dialog>` fit-content 때와 같은 이유로 위젯을 찌그러뜨리는 버그 발견·
  수정 — `flex-direction:column + justify-content:center`로 교체). (i) "타이머 열기" 런처 버튼의
  텍스트를 지우고 남은/선택된 시간 + 네이티브 `<progress>` 로 진행률을 실시간 표시 — 위젯 내부
  상태를 직접 읽지 않고 공개 이벤트(`ft:set`/`ft:tick`/`ft:statechange`/`ft:complete`/`ft:ring`)만
  구독해 구현, `aria-label="타이머 열기"`로 접근성 이름은 유지. BR로 시작→닫기→런처에 남은 시간
  실시간 반영, `extend()`로 인위적으로 20분 경과시켜 progress bar가 실제로 줄어드는 것, 6개
  테마 전환, 데스크톱/모바일 풀스크린 양쪽 실제 확인. 308/308 UT(변경 없음, 순수 CSS/데모
  변경이라 신규 테스트 불필요) + 10/10 E2E 재확인.
- **9차 라운드 — 리셋 재배치, 런처 진행률 기준 변경**(같은 세션, "20년차 UX 디자이너" 관점의
  사용자 피드백 3가지): (a) "리셋을 다시 찾기가 어려워" — v1.2에서 저빈도 "설정"으로 잘못
  분류해 톱니 패널 뒤에 숨겼던 리셋 버튼을 시작 버튼 바로 옆(항상 노출)으로 되돌렸다. 설정
  패널에는 이제 알람 미리듣기 + 테마/게이지만 남는다. (b) "초기 설정 조작·재설정이 쉽도록" —
  프리셋/시작은 이미 상시 노출이었으므로 (a)만으로 설정→실행→되돌리기→재설정 흐름 전체가
  항상 보이는 컨트롤로 완결됨을 확인, 별도 흐름 변경 없음. (c) 런처(§16.3)의 `<progress>` 기준을
  "잔여율(감소, value=remainingMs)"에서 "경과율(증가, value=totalMs-remainingMs, 0~100
  스케일)"로 변경 — 완료(remainingMs=0) 시 정확히 100%가 되도록. BR로 `extend()`를 이용해
  23분 경과(92%)와 완전 소진(100%, ringing) 양쪽 실측 확인, 설정 패널에서 리셋이 실제로
  빠졌는지 DOM으로 재확인. 308/308 UT + 10/10 E2E 재확인(리셋은 DOM 위치만 이동했고 이벤트
  배선은 동일해 회귀 없음).
- **10차 라운드 — 다이얼 preview-only, 카세트 전송 버튼, 시계 중앙 탭**(같은 세션, "분침을
  누르면 바로 시작되는데 시간 변경만 하고 시작 버튼을 눌러야 시작하게 해줘" + 전송 버튼
  이모지화 + 미세조정 FAB화 + 시계 중앙 탭 시작 요청): (a) `input/pointer.js` 를 거치는
  다이얼 커밋(클릭이든 드래그 릴리스든)이 더 이상 `autostart-on-release` 를 따르지 않고
  **항상 값만 정한다** — 프리셋 버튼만 그 속성을 그대로 따른다(§18.1). **사전 검증 중 실제
  버그 1건 발견**: `_render()` 가 `state==='setting'` 이면 시작 버튼을 `disabled` 처리하고
  있었는데, 'setting' 이 키보드 미세조정(`_previewMinutes`)·이제는 다이얼 클릭 후에도 계속
  머무르는 상태라 "미세 조정을 한 번이라도 하면 시작 버튼이 다시는 안 눌리는" 버그였다 — BR로
  `setMinutes(30)` 호출 후 시작 버튼이 `disabled:true` 로 고정되는 것을 재현 확인 후
  `disabled` 대입 자체를 제거해 수정. (b) 전송 버튼(시작/일시정지/재개/확인 + 리셋)을 텍스트
  →카세트 플레이어 이모지(▶️/⏸️/⏹️/⏮️)로, `.ft-transport`(flex:1 1 0 양쪽 동일폭)로 수평
  배치 — `aria-label` 은 한글 그대로 유지. (c) 다이얼 허브(지름 50%) 위에 투명 버튼
  (`.ft-center-start`)을 얹어 시계 중앙을 눌러도 시작/일시정지/재개/확인 — 리드아웃 텍스트를
  가리지 않으면서 같은 `toggle()` 을 공유한다. (d) "미세 조정" 텍스트+쉐브론 토글을 없애고
  유틸리티 바의 44px 스퀴클 FAB(슬라이더 아이콘)로 옮겨 목표(`+`) 버튼 바로 앞에 배치.
  BR로 눈금 근처 클릭이 값만 바꾸고(state 'setting' 유지, 자동시작 없음) 시작 버튼이 정상
  작동하는 것, 시계 중앙 탭으로 시작→일시정지, 리셋으로 idle 복귀, 조정 FAB 펼침까지 실제
  확인. 308/308 UT + 10/10 E2E 재확인(다이얼 단위테스트는 pointerdown 위치=현재값 패턴이라
  onCommit 의 autostart 제거 자체와 무관해 그대로 통과).
- **11차 라운드 — 전송 버튼 SVG 아이콘화, 일시정지 중 다이얼 재조정, 중앙 힌트**(같은 세션,
  "전송 버튼을 전체 디자인에 맞게 검정으로" + "실행 중에도 분침 누르면 멈추고 재조정" + "중앙
  버튼을 약간 투명하게 해서 인식되게" 요청): (a) ▶️/⏸️/⏹️/⏮️ 이모지가 플랫폼 폰트 색(파란
  재생 삼각형 등)에 묶여 CSS `color` 로 재정의할 수 없다는 걸 확인, 게이지/설정 아이콘과 같은
  `currentColor` SVG 4종(재생/일시정지/정지/되감기)으로 교체 — `aria-label`은 그대로 한글
  유지. (b) "실행 중 다이얼 재조정" 요청은 spec §2.2 의 7개 금지 전환 중 하나(수용 기준 7,
  전용 테스트 보호)를 뒤집는 것이라 **사용자에게 먼저 확인**(AskUserQuestion) — "일시정지
  상태에서만 허용"으로 확정. `core/machine.js` 의 `paused` 상태에 `dialdown:'setting'` 전환을
  신규 추가(`dialup`/`dialcancel`은 여전히 no-op), `running`/`ringing` 의 거부는 그대로 유지.
  `core.machine.test.js` 의 기존 테스트("dial manipulation while running (also paused /
  ringing)")가 이제 이 변경과 정면으로 배치되는 걸 확인하고 분리: running/ringing 전용 테스트는
  그대로 두고, paused 전용 신규 테스트(dialup/dialcancel=no-op, dialdown=setting 전이)를
  추가했다. `index.js`: 다이얼/프리셋/±/숫자입력의 "조작 가능" 판정을 `idle||setting` →
  `idle||setting||paused` 로 통일(`_isAdjustable()`), paused→setting 전환 시 이전 일시정지
  세션을 reset() 과 동등하게 정리(알람 취소·totalMs 초기화·저장 기록 삭제·탭 타이틀 해제)하는
  `_enterSettingIfNeeded()` 를 추가해 "화면엔 없는데 새로고침하면 옛 일시정지가 되살아나는"
  불일치를 사전에 막았다. ± 버튼이 paused 에서 처음 조정될 때는 신뢰할 수 없는 옛
  `_selectedMinutes` 대신 화면에 보이는 남은 시간을 기준으로 가감하도록 수정. (c) 시계 중앙의
  완전 투명 버튼에 허브 하단 반투명(opacity 0.35→0.65) 재생/일시정지/정지 힌트 아이콘 추가.
  BR로 (1) 실행 중 일시정지 → 다이얼 눈금 클릭 → 즉시 'setting' 전이·자동시작 없음·저장 기록
  삭제 확인 (2) 그 상태에서 시작 버튼 정상 작동 (3) running 중 다이얼 클릭은 여전히 완전히
  거부(aria-disabled=true, 값 불변)됨을 재확인해 회귀 없음을 검증 (4) 전송 버튼 아이콘이
  검정/중립색으로 보이는 것, 중앙 반투명 힌트 아이콘이 시간 숫자 아래 옅게 보이는 것 실제
  확인. 309/309 UT(신규 1개 포함) + 10/10 E2E 재확인.
- **릴리스 차단 17개** 상태: 1☑ 2☑ 4▲ 5☑(개정 기준) 9☑ 10☑ 11☐ 13☑ 17☑ 18☑ 19☑ **21☑(신규 통과)** 23☑ 27▲ 29▲(구조는 ☑) 32▲ 34☑
  — 이전 라운드 대비 **#21(다중 탭 실시간 미러링)이 실제로 구현·검증되어 ▲→☑ 로 승격**되었다.
- **Playwright E2E 스위트를 신설했다** (`test/e2e/input.drag.spec.js`, `test/e2e/runtime.multitab.spec.js`,
  총 10개, `npm run test:e2e`로 실행, 10/10 통과). 이 과정에서 실제 결함을 2건 더 발견·수정했다:
  1. **`playwright.config.js` 의 webServer 가 `demo/` 만 루트로 서빙하고 있어 `../dist/*.js` 참조가
     깨지는 설정 버그** — 프로젝트 루트 전체를 서빙하도록 수정.
  2. **키보드 화살표/Home/End/숫자입력이 idle 상태에서 단 한 번만 눌러도 즉시 자동시작해버리는 버그.**
     드래그는 "놓을 때"만 커밋되는데, 프리셋 버튼과 동일한 커밋 함수를 화살표 키에도 그대로 써서
     "미세 조정" 의미가 깨져 있었다. `_setMinutesDirect`(단발 결정: 프리셋 버튼, 드래그 릴리스 — 자동
     시작 유지)와 `_previewMinutes`(연속 조정: 화살표/Home/End/PageUp/Down/숫자입력 — 자동시작 없음)
     로 분리해 고쳤다. `ft.setMinutes()` 공개 API도 spec §10 이 `start()`를 별도 메서드로 두고 있는
     것과 일관되게 미리보기 전용으로 바꿨다(자동시작하려면 `setMinutes()` 다음에 `start()`를 명시적으로
     호출).
- **다중 탭 실시간 미러링을 새로 구현했다**: `runtime/leader.js`에 임의 브로드캐스트 메시지를 받는
  `onMessage()`를 추가하고(기존 `{isLeader, onLeaderChange, release}` 계약은 그대로 유지, 순수 추가),
  `index.js`가 매 tick·상태 전이마다 `{type:'sync', state, remainingMs, totalMs}`를 리더가 브로드캐스트
  하고 팔로워가 이를 받아 로컬 머신/스케줄을 리더와 같은 상태로 맞춘다. 오디오·알림·저장·title 은
  기존과 동일하게 리더만 수행하도록 게이팅되어 있어(팔로워가 내부적으로 start/pause 등 경로를 타도
  부작용이 없음) 이 위에 얹기만 하면 됐다.
- **가장 중요한 남은 미해결 항목**: #11(실기 백그라운드 30분), #35/#37(장시간·성능 실측),
  NVDA/VoiceOver 실기 낭독(#29) — 전부 "짧은 세션 내에 실제 디바이스·장시간 구동 없이는 검증할 수
  없는" 항목이다. 인위적으로 pass 로 표기하지 않고 ☐/▲ 로 정직하게 남겨둔다.
- `npm test`: **309/309 통과** (core 79, view 76, input 49, ports 51, runtime 54).
  `npm run test:e2e`: **10/10 통과** (Playwright, 실제 Chromium).
